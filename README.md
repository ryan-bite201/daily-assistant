# daily-assistant (retired)

This was the GitHub Pages deployment target for Daily Assistant's single-file
build. **Retired 2026-09-15** — the app now lives entirely at
**https://ryanginn.win** (Cloudflare Worker: app, API, and the published
`.ics` feed, all on one origin). GitHub Pages was disabled on this repo the
same day; this checkout is kept only as a historical artifact.

Source lives in `~/Desktop/Projects/productivity-app` (the `productivity-app`
repo). See that repo's `CLAUDE.md` for the current architecture.
